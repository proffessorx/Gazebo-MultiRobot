<?xml version="1.0" encoding="UTF-8"?>
<launch>		

  <!-- Map server -->
  <arg name="robot" />
  <arg name="map_file" default="$(find mybot_gazebo)/maps/robotino3/world.yaml"/>
  <node name="map_server" pkg="map_server" type="map_server" args="$(arg map_file)" />

  <arg name="initial_pose_x" default="0.0"/> <!-- Use 17.0 for willow's map in simulation -->
  <arg name="initial_pose_y" default="0.0"/> <!-- Use 17.0 for willow's map in simulation -->
  <arg name="initial_pose_a" default="0.0"/>

  <!--- Run fake_localization -->

  <node pkg="fake_localization" type="fake_localization" name="fake_localization" output="screen">
	<param name="odom_frame_id" value="$(arg robot)/odom"/>
<!--
	<param name="delta_x" value="0.0"/>
	<param name="delta_y" value="0.0"/>
	<param name="delta_yaw" value="0.0"/>
	<param name="global_frame_id" value="/map"/>
-->
	<param name="base_frame_id" value="$(arg robot)/base_link"/> 
	<remap from="base_pose_ground_truth" to="odom"/> 
  </node> 
  <!--- Run move_base -->
  <node pkg="move_base" type="move_base" respawn="false" name="move_base" output="screen">
        <rosparam file="$(find mir_nav2d)/cfg/costmap_common_params.yaml" command="load" ns="global_costmap" />
        <rosparam file="$(find mir_nav2d)/cfg/costmap_common_params.yaml" command="load" ns="local_costmap" />
        <rosparam file="$(find mir_nav2d)/cfg/local_costmap_params.yaml" command="load" />
        <rosparam file="$(find mir_nav2d)/cfg/global_costmap_params.yaml" command="load" />
        <rosparam file="$(find mir_nav2d)/cfg/base_local_planner_params.yaml" command="load" />
		  <rosparam file="$(find mir_nav2d)/cfg/move_base_params.yaml" command="load" />  <!--
	<param name="global_costmap/robot_base_frame" value="base_link"/>	
	<param name="local_costmap/global_frame" value="odom"/>
	<param name="local_costmap/robot_base_frame" value="base_link"/> -->
  </node>

</launch>
